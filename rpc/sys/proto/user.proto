syntax = "proto3";

package main;

option go_package = "./proto";

message LoginReq {
  string account = 1;
  string password = 2;
}

message loginResp {
  int64 id = 1;
  string AccessToken = 3;
}

service UserService {
  // 用户登录
  rpc Login(LoginReq) returns(loginResp);
}